#lauch venv
``` source venv/bin/activate```

#install requirement
``` pip install -r requirement.txt```

#lauch server
``` uvicorn main:app --reload ```

#if unable to lauch test
``` PYTHONPATH=. pytest```